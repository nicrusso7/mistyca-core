package bot.reasoning

import bot.knowledge.adaptation.model.Intent
import bot.knowledge.adaptation.model.Action

object IntentAdaptation {
  
  def performAction(intent:Intent) : Action = {
    return null;
  }
}